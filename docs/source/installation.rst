Installation
============

From source
-----------

.. code-block:: bash

   git clone https://github.com/agitfirat/TP_packaging.git
   cd TP_packaging
   python -m venv .venv
   source .venv/bin/activate
   pip install -e .[dev]
